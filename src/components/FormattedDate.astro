---
export interface Props {
	date: Date
}

const { date } = Astro.props
---

<time class='text-sm font-bold' datetime={date.toISOString()}>
	{
		date.toLocaleDateString('de-DE', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
