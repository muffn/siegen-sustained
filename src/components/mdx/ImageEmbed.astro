---
import { adjustImageUrl } from '../../utils/fixUrl'
const props = Astro.props

type Props = {
	image: string
	alt: string
}

const adjustedImage = adjustImageUrl(props.image)
---

<img src={adjustedImage} alt={props.alt} class='w-full' />
<slot />
